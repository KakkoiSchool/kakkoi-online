/**
 * Stage 3 — someone to fight when nobody else is here (A21). Not built yet.
 *
 * The point of the NPC is that it answers exactly the same questions a remote
 * peer does, so `duel.js` cannot tell the difference and there is no second
 * code path for "playing alone".
 */
export const NOT_BUILT_YET = 'stage 3';
