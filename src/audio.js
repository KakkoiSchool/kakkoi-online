/**
 * Sound. Off by default, with an obvious control — a page may not make noise
 * nobody asked for, and a browser will refuse to play before the first click
 * anyway (A17). Files are in `audio/`.
 */
export const NOT_BUILT_YET = 'stage 4';
