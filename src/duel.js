/**
 * Stage 3 — the fight. Not built yet.
 *
 * The challenge state machine (A19), the three moves (A20) and commit–reveal
 * over the wire (A22). Both players must compute the same result from the same
 * inputs, which is why every rule lives in `battle/rules.js` and this file only
 * decides WHEN to ask it.
 */
export const NOT_BUILT_YET = 'stage 3';
